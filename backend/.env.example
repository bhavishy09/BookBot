# Database — use PostgreSQL in production
DATABASE_URL=sqlite:///./appointment_booking.db

# JWT secret (change in production!)
SECRET_KEY=change-me-in-production-use-a-long-random-string

# Gemini API key (set in Phase 3)
GEMINI_API_KEY=
